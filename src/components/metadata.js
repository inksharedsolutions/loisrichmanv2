import React from 'react';
import { Helmet } from 'react-helmet';

const Metadata = () => {
	return <Helmet titleTemplate="%s | Lois Richman" defaultTitle="Lois Richman" />;
};

export default Metadata;
